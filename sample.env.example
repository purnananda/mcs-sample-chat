# Copilot Studio Configuration
# Copy this file to .env and fill in your actual values

# Environment ID from Copilot Studio (Settings => Advanced => Metadata)
VITE_ENVIRONMENT_ID=""

# Schema name from Copilot Studio (Settings => Advanced => Metadata)
VITE_AGENT_IDENTIFIER=""

# Azure App Registration Details
VITE_TENANT_ID=""       # Directory (tenant) ID from Azure Portal
VITE_APP_CLIENT_ID=""   # Application (client) ID from Azure Portal

# Optional: Custom configuration
# VITE_REDIRECT_URI="http://localhost:3000"  # Default redirect URI

# Note: Use VITE_ prefix for Vite to expose these variables to the client
# These values will be visible in the browser, so only use non-sensitive defaults